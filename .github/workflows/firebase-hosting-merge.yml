# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - master
env:
  CONTENTFUL_DELIVERY_TOKEN: ${{ secrets.CONTENTFUL_DELIVERY_TOKEN }}
  CONTENTFUL_SPACE: ${{ secrets.CONTENTFUL_SPACE }}
  CONTENTFUL_PREVIEW_TOKEN: ${{ secrets.CONTENTFUL_PREVIEW_TOKEN }}
  GTM_ID: ${{ secrets.GTM_ID }}
  MAILCHIMP_KEY: ${{ secrets.MAILCHIMP_KEY }}
  GATSBY_SLACK_WEBHOOK_URL: ${{ secrets.GATSBY_SLACK_WEBHOOK_URL }}
  GATSBY_RECAPTCHA_SITE_KEY: ${{ secrets.GATSBY_RECAPTCHA_SITE_KEY }}
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: yarn install && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_A_TEAM_10A0E }}'
          channelId: live
          projectId: a-team-10a0e
